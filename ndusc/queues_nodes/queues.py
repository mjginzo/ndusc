# -*- coding: utf-8 -*-
"""Module with different queues rules to determine node list to execute."""


def method1(tree, id):
    """."""
    node = tree.get_node(id)
    node['solution']
    return []
